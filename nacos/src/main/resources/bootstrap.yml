server:
  port: 8089
spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
        name: application-nacos.yml
        server-addr:

management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    tags:
      application: nacos-provider